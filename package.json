{
  "name": "comp-network-chat",
  "private": true,
  "scripts": {
    "frontend:dev": "bun --filter 'network-project-frontend' dev",
    "frontend:prepare": "husky packages/web",
    "socket:prepare": "husky packages/socket",
    "prepare": "husky"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "realm-web": "^2.0.0"
  }
}
